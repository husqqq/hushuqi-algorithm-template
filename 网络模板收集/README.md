# 网络模板收集

这里只存放从外部收集的竞赛资料，用于比对、查漏和学习，不是 `hushuqi算法竞赛模板` 的源码组成部分。

大部分资料来自“菜菜园子”群文件，群号为 `730602769`。`Markdown/` 中的文本主要由工具从 PDF 转换得到，转换结果可能存在公式丢失、表格错位、代码缩进变化、乱码或字符识别错误；使用时应与 `PDF/` 中的原件交叉核对。

- `Markdown/`：可检索的文本版模板。
- `PDF/`：原始 PDF 资料。
- `源码/`：原始 TeX、分文件模板和单独代码。
