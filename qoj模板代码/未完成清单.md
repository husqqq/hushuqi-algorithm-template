# QOJ 3936 未完成清单

以下题目不是简单的输入输出差异；当前仓库中的相关模板不足以直接组成完整、可验证的 QOJ 提交，因此没有生成误导性的 `main.cpp`。

| 题号 | 题目 | 缺少的完整能力 |
| --- | --- | --- |
| 3 | 基本子串字典 | 子串 border 的最短值、最长值与数量的批量查询结构 |
| 5 | 广义后缀自动机 | 多串去重建图和本质不同子串统计入口 |
| 9 | 区间本质不同子串 | `2×10^5` 次区间询问的离线后缀结构 |
| 16b | 最短路 2 | `n=5000,m=3×10^5` 下同时求负权最短路、不可达点与负无穷点的高效实现；现有 Bellman–Ford 最坏复杂度不足 |
| 1H | 有源汇有上下界最小费用最大流 | 下界可行流、最大流和负费用最优化的完整组合 |
| 41 | 类欧几里得算法 | `x^k1 floor((ax+b)/c)^k2`（总次数不超过 10）的通用递归 |
| 44 | Tutte 多项式 | 完整二元 Tutte 多项式求值；现有模板仅覆盖色多项式特化 |
| 49 | Min_25 筛 | 题目指定 `f(p^c)=p xor c` 的素数幂贡献与求和实现 |
| 4A | Determinant of A+Bz | 奇异 `B` 情况下完整的多项式矩阵行列式算法 |
| 4D | Equation Mod 3 | `n,m≤2000` 的三元域位并行高斯消元；现有普通矩阵消元最坏复杂度不足 |
| 51 | 四维偏序 | 严格四维点对计数的嵌套离线分治 |
| 60 | 动态树 - 简单版 | 支持不存在的 link/cut、连通性及路径点权 xor 的完整 LCT 入口 |
| 61 | 点分治 | 多询问树上指定路径长度存在性的完整离线流程 |
| 64 | Historical Maximum Value | 区间加、区间赋值、当前最大值和历史最大值的复合懒标记 |
| 94 | 稀疏 XOR 卷积 | 利用每个因子稀疏支持和题目特殊模数的算法；普通 FWT 不满足约束 |
| EX1 | Kth 1 | 按约定不处理 |

## 特殊题

- 46 Online Multiplicative Inverse Query 是函数接口题。QOJ 要求包含 `inv.h` 并实现 `void init(int p)` 与 `int inv(int x)`，不应提供 `main.cpp`。
