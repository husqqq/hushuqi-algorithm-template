# 待加入正式模板

本文件只登记 QOJ 候选区中已经实现和验证、但尚未获得批准写入 `hushuqi算法竞赛模板` 的代码。带 `pending_template: true` 的条目由 `tools/generate.py` 保留其本地打印版 `template.hpp`，运行生成器不会用正式模板展开结果覆盖它。

| 题号 | 候选接口 | 拟加入章节 | 来源与改动 | 复杂度 | 验证 |
| --- | --- | --- | --- | --- | --- |
| 3 | `basicDict(s, queryList)` | 4.3.011 基本子串字典 | 按 `k.cpp` 改为离线后缀数组倍增，批量处理询问并用等差数列交集统计真 border | 预处理与全部询问约 `O((n+q) log n)` | 随机区间与朴素 border 对拍；候选入口已编译 |
| 1G | `triWeight998` | 5.9.006 三元环、四元环计数 | 基于正式模板按度定向；将同一中间点的公共邻点权先汇总，最后统一取模；修复参考代码在源点编号为 0 时的时间戳冲突 | `O(m sqrt(m))` 时间，`O(n+m)` 空间 | 3000 张小图三点暴力；约 10 万边完整图较旧实现约快 4 倍 |
| 1H | `NetworkSimplexBFlow`、`lowerBoundMinCostMaxFlow` | 5.8.011 费用缩放最小费用 b 流与带费用上下界 | 参考 `k.cpp` 的 network simplex 核心重新封装；先由 Dinic 确定最大流值，再从零建立固定供需最小费用问题，避免复用残量网络和依赖人工边下标；罚值由输入费用动态计算 | Dinic 加 network simplex，后者最坏复杂度不作多项式保证，随机数据上快 | 3000 组小容量全流量枚举；已知反例候选输出 `0 1`，参考代码错误输出 `1 6000000000`；`n=400,m=10000` 随机图约快 5.8 倍 |
| 1I | `chordalCert` | 5.10.001 弦图、完美消除序列与判错 | 基于正式模板 MCS/证书接口的桶式 MCS；使用 CSR 邻接、压平子结点、线性 PEO 验证，并只在失败时做一次 BFS | `O(n+m)` 时间和空间 | 5000 张随机图逐一检查 PEO 或诱导环证书；`n=2e5,m约4e5` 约 0.18 秒，旧候选约 0.41 秒 |
| 9 | `rangeSubs(s, queryList)` | 4.3.010 区间本质不同子串 | 离线 SAM、后缀链接树，区间和复用正式 `LazySeg<Info,Tag>`；前缀新增贡献改用等差数列闭式，线段树只维护负修正，避免每个右端点一次的整段加一 | 当前 `O(n log^2 n+q log n)`，空间 `O(n)`；实测随机 `n=q=2e5` 约 `0.282s`；后续研究 LCT/HLD 颜色段维护 | 随机区间对拍、题面样例、三类满规模基准均通过 |
| 41 | `floorMoment`、`floorMoments` | 7.5.003 类欧几里得 | 次数不超过 10 的混合矩表递推打印版候选，已拆出算法输入与返回值 | 递归次数与次数上界相关 | 固定边界校验与入口编译 |
| 44 | `tutteEval` | 5.10.010 图多项式 | 集合幂级数指数求 Tutte 点值的打印版候选，已拆出图输入与核心计算 | `O(n^2 2^n)` 级别 | 小图枚举与入口编译 |
| 4A | `detAPlusBz` | 9.1.007 特征多项式 | 多项式矩阵消元处理奇异 `B` 的打印版候选，返回多项式系数 | `O(n^3)` | 固定矩阵校验与入口编译 |
| 4D | `solveMod3` | 9.2.001 线性方程组 | 三元域位并行高斯消元打印版候选，返回一组解并明确可解前置条件 | `O(n^3 / word)` | 固定可解校验与入口编译 |
| 51 | `countFourDimensional` | 2.2.005 高维偏序 | 将嵌套离线分治与 Fenwick 主体提升为候选 HPP | 行为与原 QOJ 入口一致 | 候选展开后的单文件入口编译通过 |
| 64 | `SegmentTree` | 3.3.003 懒标记线段树 | 当前值与历史峰值的可组合懒标记打印版候选，使用 0 下标左闭右开接口 | `O((n+q)log n)` | 固定操作序列与入口编译 |
| 16b | `shortestPathWithNegInf` | 5.2.002 最短路与负无穷传播 | 研究 `k.cpp` 的最短路树维护、回边判环和后继传播，重写为 0 下标算法接口；正式 Bellman-Ford 只返回一组负环且不传播负无穷 | 实际队列式增量松弛；最坏复杂度不作多项式保证 | 5000 组随机图与 Bellman-Ford 分类对拍 |
| 46 | `init`、`inv` | 7.1.001 在线模逆元 | 研究 `k.cpp` 的分数树索引与递推表，加入固定模 `998244353` 的摊还 O(1) 求逆；保留紧凑表项 | 预处理 O(2^22)，单次 O(1)，空间 O(2^22) | 100000 个随机非零元逐项验证 |
| 94 | `sparseXorConvolution` | 10.1.006 稀疏 XOR 卷积 | 研究 `k.cpp` 的局部 Walsh、平方根分解和全局逆变换，重写为稀疏因子接口；普通 FWT 不满足题目约束 | `O(2^n n + sum 2^d d)` 域运算，空间同规模 | 100 组随机稀疏因子与朴素 XOR 卷积核对 |

## 正式模板已有条目待回写

这张表记录“QOJ 修正已经说明正式模板现有条目需要修改或补充”的事项。它们即使不新增知识点，也必须在用户确认提交结果后回写正式模板源码、正文和测试，不能只改 QOJ `main.cpp`。

| 优先级 | 题号 | 正式条目 | 性质 | 拟回写内容 | 回写前门槛 |
| --- | --- | --- | --- | --- | --- |
| 高 | 18 | 5.6.003 Hopcroft-Karp | 性能优化 | 标准版改为 CSR 连续邻接表、32 位热点状态和复用 BFS 队列；另保留 `random.cpp` 的多源 BFS 立即增广版本，随机数据下通常更快但最坏复杂度不如 Hopcroft-Karp | 两版均通过随机暴力匹配；同规模均匀随机图本机中位数约 0.084 秒 / 0.078 秒，待用户决定正式模板接口 |
| 高 | 40b | 7.2.004 Pollard-Rho 分解 | 性能缺陷 | 将逐步 `gcd` 的 Floyd 版本换成 Brent 分块乘积与批量 `gcd`，保留 `g==n` 的逐步回退 | 先在 QOJ 目录补受保护候选 HPP；与试除/现有 `factor64` 随机对拍，并覆盖平方、大半素数和接近 `2^64` 的输入 |
| 高 | 83 | 11.3.006 旋转卡壳 | 疑似边界正确性 | `diameter2` 在平行支撑边时同时检查 `j` 与 `j+1`；若确认必要，`farthestPair` 也必须同步，不能只修一个接口 | 当前仅有 QOJ WA 与参考补丁，随机严格凸包尚未构造出反例；必须先取得 QOJ 结果并加入可复现退化多边形用例 |
| 中 | 4C | 9.2.001 F2 线性方程组 | 专用高性能接口 | 保留返回特解和零空间基的通用 `gaussF2`，另加只求一组解的“前向消元 + 回代”位并行接口，避免全 Gauss-Jordan 和基构造 | 先整理候选 HPP；补可解、无解、欠定和随机小矩阵暴力验证，不能直接照搬题目保证可解的入口 |
| 中 | 15、1H | 5.8.004、5.8.011 最小费用流 | 算法备选 | 保留势函数 Dijkstra 与费用缩放接口，新增 network simplex 作为随机稠密/大容量数据的备选，并复用已验证的固定供需封装 | 1H 候选已过 3000 组全流量枚举和已知反例；等待 QOJ 后再决定章节位置，不替换已有 AC 接口 |
| 中 | 1G | 5.9.006 加权三元环 | 写法优化 | 把公共邻点权先按中间点汇总、最后取模的固定模数快速接口回写；保留通用 `weightedTri` | 已过 3000 张图暴力；等待 QOJ，并补 0 号点时间戳回归 |
| 中 | 1I | 5.10.001 弦图判定 | 复杂度改进 | 用桶式 MCS、CSR 邻接和压平子结点替换大图证书接口的有序集合版本，保留教学版接口 | 已过 5000 张图证书检查；等待 QOJ，并补非连通图与长诱导环用例 |
| 低 | 45 | 13.1.009 Nim Product | 接口易用性 | 增加会自行调用 `init()` 的安全 32 位乘法封装，或把 `p16/p32/p32high` 明确标为需先初始化的底层接口 | 正式 64 位 `nimProduct` 本身正确；仅需验证新包装与 `product` 低 32 位结果一致 |

## 确认无需回写

| 题号 | 结论 |
| --- | --- |
| 26、27 | 现有快速多项式复合逆与 Bostan-Mori 渐近复杂度正确，差异只在 NTT、编译器和内存布局常数，不做纯卡常改写 |
| 45 的算法主体 | QOJ TLE 的直接原因是入口在 32 位数据上调用完整 64 位 `nimProduct`；正式模板已有更快的 `p32` 核心，不需要替换 Nim 积算法，只需改善初始化封装/文档 |
| 4C 的快读 | `fread` 输入器只属于 QOJ 数据规模适配，不进入线性代数模板；只记录位并行“求任意一组解”接口 |

## 使用约定

- 表中仍待批准的 `template.hpp` 只属于 QOJ 候选区；B Runs 已由提交 2830439 验证并同步正式模板，不再列入待回写表。
- 候选目录至少保留 `README.md`、`template.hpp`、性能验证用 `main.cpp`；已有提交记录的题目同时保留 `提交记录.md`。`main.cpp` 是已经提交的性能版，不再由中间源码展开；问题状态统一见 `未完成清单.md` 和 `QOJ整理与正式模板计划.md`。
- 3、9、41、44、4A、4D、51、64 已先整理为打印版算法接口；用户确认候选提交和接口后，再迁入正式章节。1G、1H、1I 的性能入口与打印版候选也保持分离。
- Runs 的单模哈希实现已经进入正式模板；正文保留概率碰撞风险，不把它描述为确定性算法。
- 用户确认候选提交通过后，再把接口、说明和测试统一迁入拟加入章节，并移除 `pending_template` 标记。
