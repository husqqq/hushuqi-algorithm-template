# 第 01 章基础与工程审计

固定官方题库 commit `04c8de378bab67be926325de2871f0babb8e6451` 的 `sample/` 有 3 道正式题，均由第 01 章主责。本表冻结全集，不以当前候选目录定义范围。

| Official problem | Template mapping / API | Header / dependency / test | Reference source | Current source | Source evidence | Local state | Online state |
|---|---|---|---|---|---|---|---|
| `sample/aplusb` | `1.1.002`; template entry adapter | `include/base.hpp`; `tests/test_ch01_ch02.cpp` | `library-checker-solutions/sample/aplusb/main.cpp` | `sample/aplusb/main.cpp` | `template_solution` provenance; current source SHA256; `46a4955570b3...` | `current_verified` | `online_ac #395496`, `0.009s` |
| `sample/many_aplusb` | `1.2.001`; template entry adapter | `include/base.hpp`; `tests/test_ch01_revised.cpp` | `library-checker-solutions/sample/many_aplusb/main.cpp` | `sample/many_aplusb/main.cpp` | `template_solution` provenance; current source SHA256; `810705898261...` | `current_verified` | `online_ac #395498`, `0.121s` |
| `sample/many_aplusb_128bit` | `1.2.002`; template entry adapter | `include/base.hpp`; `tests/test_ch01_revised.cpp` | `library-checker-solutions/sample/many_aplusb_128bit/main.cpp` | `sample/many_aplusb_128bit/main.cpp` | `template_solution` provenance; current source SHA256; `e40327a60c1d...` | `current_verified` | `online_ac #395499`, `0.36s` |

## 审计结论

- 三题均已逐行对照官方题面、checker、`library-checker-solutions` 参考和当前 `base.hpp` 调用链；模板无需修改，现有 `cin/cout`、`FastInput` 与 `parseInt128/toString` 分别覆盖题意、边界和复杂度。
- `tools/generate_ch01_lc.py --check` 通过，三份 `main.cpp` 均由当前用户模板确定性展开生成；三份 scoped receipt 均通过官方 sample、显式边界/官方边界、固定种子 random 与 checker。
- 性能门已完成五轮交替测试；`aplusb` 的 `2.0x` 阈值仅用于记录单输入 I/O 启动常数，渐进复杂度仍为 `O(1)`，其他两题使用默认 `1.25x`。
- 三题已按单题串行顺序提交，均为当前源码 `online_ac`；服务端回显源码 SHA256 与各自 `main.cpp` 一致，无需进一步反思或重交。
