# Library Checker `other` Category Audit

固定官方题库 commit `04c8de378bab67be926325de2871f0babb8e6451` 的 `other/` 有 7 个直属题目目录。本表是它们的唯一章节归属索引；每道题只出现一次。归属按《90-模板复查与受控提交流程.md》的“决定答案语义和目标复杂度的核心算法”规则判定，而不是按候选实现可能使用的辅助数据结构判定。

| Official problem | Assigned chapter | Core template interface / current audit target | Cross-chapter dependencies | Assignment basis | Current state |
|---|---:|---|---|---|---|
| `other/consecutive_terms_of_linear_recurrent_sequence` | 10 | 10.4.006 `ratBlock` | 10.1.003 NTT, 10.2 Poly | 求线性递推远处连续项，核心是有理生成函数的连续系数算法。 | `audited` |
| `other/find_linear_recurrence` | 10 | 10.4.001 `berlekampMassey` | 7.1 模整数 | 求最短线性递推，核心是 Berlekamp--Massey。 | `audited` |
| `other/kth_term_of_linearly_recurrent_sequence` | 10 | 10.4.005 `bostanMori` / 10.4.003 `linearRec` | 10.1.003 NTT, 10.2 Poly | 求线性递推的远项，核心是递推转有理式与 Bostan--Mori。 | `audited` |
| `other/longest_increasing_subsequence` | 02 | 2.2.010 `lis` | none | 输出严格 LIS 的下标，核心是不变量为最小末值的 LIS。 | `audited` |
| `other/sum_of_exponential_times_polynomial` | 10 | Chapter 10 polynomial / rational-generating-function audit | 7.1 模整数 | 指数乘多项式幂和在 $d\le 10^7$ 下需多项式与生成函数算法，不是数据结构题。 | `audited` |
| `other/sum_of_exponential_times_polynomial_limit` | 10 | Chapter 10 polynomial / rational-generating-function audit | 7.1 模整数 | 无穷指数乘多项式幂和需多项式/生成函数计算，不是数据结构题。 | `audited` |
| `other/two_sat` | 05 | 5.4.001 `TarjanTwoSAT` | 5.3 SCC | 核心是蕴含图 SCC 的可满足性和构造。 | `audited` |

## Scope result

本表完整覆盖冻结 commit 的 `other/` 目录，无重复、无未分配行。第 03 章获分配题数为 0；其最终 LC 覆盖集合因此是主责分类 `data_structure/` 的 47 题，且不因其他章节使用第 03 章公共数据结构而改变。

本文件只冻结归属，不把任何题目提前标为模板已映射、已生成或已验证。每个主责章节仍须在自己的覆盖矩阵中完成完整的源码级审阅、模板订正、独立源码生成、本地验证和在线提交闭环。
