# 第 12 章动态规划与优化审计

当前冻结 commit 的 251 道正式 Library Checker 题在 `LC_TEMPLATE_MAPPING.tsv` 中没有分配给第 12 章。本章仍必须审计模板正文、同步头文件、公共依赖与 `tests/test_ch12_*.cpp`；“没有主责 LC 题”不等于正文或测试无需审阅。

## LC 覆盖结论

| Official problem | 状态 | 依据 |
|---|---|---|
| `null` | `not_applicable` | TSV 中 `chapter=12` 的行数为 0；冻结 commit 的 `other/` 已在 `93-其他分类审计.md` 全量且唯一分配，未向第 12 章分配题目。 |

本章不存在可生成或可提交的 LC 候选；若以后发现官方题应由动态规划或优化主责，必须先修订 `93-其他分类审计.md` 或分类规则、通过映射校验，再在本文件建立逐题 `review_pending` 行。
