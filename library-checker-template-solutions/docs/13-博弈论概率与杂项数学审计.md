# 第 13 章博弈论、概率与杂项数学审计

当前冻结 commit 的 251 道正式 Library Checker 题在 `LC_TEMPLATE_MAPPING.tsv` 中没有分配给第 13 章。本章仍必须审计模板正文、同步头文件、公共依赖与 `tests/test_ch13_*.cpp`；没有主责 LC 题不能替代模板审阅。

## LC 覆盖结论

| Official problem | 状态 | 依据 |
|---|---|---|
| `null` | `not_applicable` | TSV 中 `chapter=13` 的行数为 0；冻结 commit 的 `other/` 已在 `93-其他分类审计.md` 全量且唯一分配，未向第 13 章分配题目。 |

将来出现主责为博弈论、概率或本章杂项数学的官方题时，必须先在唯一映射表中分配章节并通过校验，再建立逐题矩阵，不能以本次空范围推断长期无任务。
